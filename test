pipeline {
    agent any
    environment { 
        fname = 'kamallls'
    }
    stages {
        stage('Example') {
            steps {
                sh 'echo "HELLO ${fname}"'
            }
        }
    }
}